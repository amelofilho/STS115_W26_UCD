# Lecture 2: 1/16

### Commands:

- git --version
- git config
    - `--list`
    - `--global core.editor "nano"`
- git init Repo_Name
- git status
- Pushing changes:
- git add
- git commit
    - if no string is passed the default text editor should open...
- git push

### Git (Version Control System):

**.git files:**

Hidden config file created upon creating a repo from directory. 

---

